'use client'

import { Button } from '@/components/ui/button'

export function AddAgreementButton() {
  return (
    <Button size="sm" onClick={() => alert('新增协议功能开发中')}>
      新增协议
    </Button>
  )
}
